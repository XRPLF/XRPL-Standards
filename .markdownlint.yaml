# Markdownlint configuration for XRPL Standards repository
# Rule reference: https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md

default: true

# MD001: Heading levels should only increment by one level at a time
# Disabled: Specs often skip heading levels for document structure
MD001: false

# MD003: Heading style - enforce ATX style (# Heading)
MD003:
  style: atx

# MD004: Unordered list style - enforce dashes
MD004:
  style: dash

# MD007: Unordered list indentation - 2 spaces
MD007:
  indent: 2

# MD013: Line length limit
# Configured: Keep prose reasonably readable without fighting tables/code/URLs
MD013:
  line_length: 120
  heading_line_length: 120
  code_blocks: false
  tables: false

# MD024: Multiple headings with the same content
# Configured: Only check siblings (allows same heading in different sections)
MD024:
  siblings_only: true

# MD025: Multiple top-level headings in the same document
# Disabled: Some specs have multiple H1s for structure
MD025: false

# MD028: Blank line inside blockquote
# Disabled: Multi-paragraph blockquotes are valid
# MD028: false

# MD029: Ordered list item prefix (1. 2. 3. vs 1. 1. 1.)
# Configured: Allow either repeated 1. or explicit ordered numbering
MD029:
  style: one_or_ordered

# MD030: Spaces after list markers
# Configured: Enforce one space after list markers
MD030:
  ul_single: 1
  ol_single: 1
  ul_multi: 1
  ol_multi: 1

# MD033: Inline HTML
# Disabled: Specs use HTML plus angle-bracket placeholders and metadata
MD033: false

# MD034: Bare URL used
# Disabled: Bare URLs are common in technical references
MD034: false

# MD036: Emphasis used instead of a heading
# Disabled: Specs use bold text for sub-section labels
MD036: false

# MD038: Spaces inside code span elements
# Enabled: Usually indicates accidental spacing that affects rendered Markdown
MD038: true

# MD039: Spaces inside link text
MD039: true

# MD040: Fenced code blocks should have a language specified
# Configured: Require explicit language, using "text" for plain examples
MD040: true

# MD041: First line in file should be a top-level heading
# Disabled: XLS specs start with HTML metadata block
MD041: false

# MD045: Images should have alternate text (alt text)
# Enabled: Improves accessibility for diagrams and screenshots
MD045: true

# MD051: Link fragments should be valid
# Enabled: Catches broken intra-document heading links
MD051: true

# MD055: Table pipe style
# Configured: Leading and trailing pipes improve readability and portability
MD055:
  style: leading_and_trailing

# MD058: Tables should be surrounded by blank lines
# Enabled: Helps parsers reliably detect tables
MD058: true

# MD059: Link text should be descriptive
# Enabled: Improves accessibility and scanability
MD059: true

# MD060: Table column style (alignment)
# Configured: Allow aligned/compact/tight table styles, but reject malformed ones
MD060:
  style: any
