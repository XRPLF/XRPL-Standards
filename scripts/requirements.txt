# Python dependencies for XLS Standards site generation and validation
#
# Core site generation dependencies
markdown>=3.10.2,<4.0.0    # Markdown to HTML conversion with extensions
jinja2>=3.1.6,<4.0.0      # HTML template engine
beautifulsoup4>=4.14.3,<5.0.0  # HTML/XML parsing and metadata extraction
pyyaml>=6.0.3,<7.0          # YAML configuration parsing
markdown-it-py>=4.0.0,<5.0.0  # Markdown parser with AST support for validation

# Additional dependencies for enhanced markdown processing
# Note: These are included automatically with markdown[extra] but listed for clarity
# - tables: GitHub-flavored markdown tables
# - codehilite: Syntax highlighting for code blocks
# - toc: Table of contents generation
# - extra: Additional markdown extensions bundle
